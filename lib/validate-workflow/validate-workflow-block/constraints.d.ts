declare const ON = "on";
declare const RESOLVES = "resolves";
export declare const AVAIABLE_ATTRIBUTES: string[];
export declare const ATTRIBUTES_VALUES: {
    [ON]: (value: any) => boolean;
    [RESOLVES]: (value: any) => boolean;
};
export {};
